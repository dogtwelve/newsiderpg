#include "WIPIheader.h"

#if defined(time)
	#undef time
#endif

#if defined(exp)
	#undef exp
#endif

#if defined(sin)
	#undef sin
#endif

#if defined(cos)
	#undef cos
#endif

#if defined(ColorType)
	#undef ColorType
#endif


#if defined(setClip)
	#undef setClip
#endif


#if defined(setColor)
	#undef setColor
#endif


#if defined(drawLine)
	#undef drawLine
#endif

#if defined(clone)
	#undef clone
#endif

#if defined(setX)
	#undef setX
#endif

#if defined(setY)
	#undef setY
#endif

#if defined(setZ)
	#undef setZ
#endif

#if defined(draw)
	#undef draw
#endif

#if defined(move)
	#undef move
#endif

#if defined(update)
	#undef update
#endif

#if defined(setPosition)
	#undef setPosition
#endif

#if defined(setState)
	#undef setState
#endif

#if defined(components)
	#undef components
#endif

#if defined(add)
	#undef add
#endif

